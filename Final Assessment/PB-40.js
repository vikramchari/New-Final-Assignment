// Multi parameter, multi line code:
let sum = (a, b,c) => {  
    let result = a + b+c;
    return result; 
  };
  
  alert( sum(1, 2,3) ); // 6


//No parameter, single line code:

let a = () => "hi"
alert(a())
  

//One parameter, single line code:
  let double = n => n * 2;

  alert( double(3) ); // 6